import { FilePreviewTplDirective } from './file-preview-tpl.directive';

describe('FilePreviewTplDirective', () => {
  it('should create an instance', () => {
    const directive = new FilePreviewTplDirective();
    expect(directive).toBeTruthy();
  });
});
