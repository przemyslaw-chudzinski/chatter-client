import { OpenFileBoxDirective } from './open-file-box.directive';

describe('OpenFileBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenFileBoxDirective();
    expect(directive).toBeTruthy();
  });
});
