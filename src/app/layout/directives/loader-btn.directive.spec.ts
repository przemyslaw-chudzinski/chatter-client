import { LoaderBtnDirective } from './loader-btn.directive';

describe('LoaderBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new LoaderBtnDirective();
    expect(directive).toBeTruthy();
  });
});
