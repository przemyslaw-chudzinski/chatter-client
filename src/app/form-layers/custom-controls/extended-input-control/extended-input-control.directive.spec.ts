import { ExtendedInputControlDirective } from './extended-input-control.directive';

describe('ExtendedInputControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtendedInputControlDirective();
    expect(directive).toBeTruthy();
  });
});
