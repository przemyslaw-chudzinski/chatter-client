import { IfUserEqualsDirective } from './if-user-equals.directive';

describe('IfUserEqualsDirective', () => {
  it('should create an instance', () => {
    const directive = new IfUserEqualsDirective();
    expect(directive).toBeTruthy();
  });
});
