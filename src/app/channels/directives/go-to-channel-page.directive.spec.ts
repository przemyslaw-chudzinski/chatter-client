import { GoToChannelPageDirective } from './go-to-channel-page.directive';

describe('GoToChannelPageDirective', () => {
  it('should create an instance', () => {
    const directive = new GoToChannelPageDirective();
    expect(directive).toBeTruthy();
  });
});
